language: node_js
node_js:
  - "12.3.1"

branches:
  only:
    - master

addons:
  chrome: stable

before_script:
  - npm install -g @angular/cli

cache:
  directories:
    - node_modules

script:
  - ng build --prod
deploy:
  provider: script
  script: bash travis/push.sh
  on:
